# PR25 이미지·전투 후속 패치

기준: PR25 45aefe8c 및 사용자 제공 RACE_PLAN_CODEX_SPEC_v2.md. 원본 문서와 저장소 문서는 줄바꿈을 정규화한 뒤 동일함을 확인했다.

## 이미지 및 연출

imagegen으로 배경 3종(1920×1080), 라스트 3F 배너(960×180), 출발 게이트·승부처 링·라스트 3F VFX 시트 3종(1920×192, 10프레임)을 제작했다. 생성 후에는 크기·프레임 배치만 기계적으로 처리했다. 원본 프롬프트와 소스/최종 SHA-256은 [provenance.json](art-source/race-plan/provenance.json)에 보관한다.

선택창·출주표·전투 HUD와 Phaser 재생기에 연결했다. VFX는 출주마 주변에만 짧게 재생되며, 모션 줄이기 설정에서는 확대/반복을 줄인다. 이미지 요청 실패 시 바탕색과 도형 폴백을 유지한다. 기존 21종 SVG 아이콘과 캐릭터 모션은 유지한다.

## 전투 및 사용성 수정

- 지구력 스택이 같은 오라를 덮어써 6중첩도 1중첩 성능이 되던 오류를 노드별 누적 수치 갱신으로 수정했다.
- 한 번의 공격으로 여러 진행도 경계를 넘으면 각 페이즈를 순서대로 한 번씩 처리한다. 승부처 보상 누락을 방지한다.
- 장거리 승부수의 라스트 3F 지구력 비례 AD/AP 보상과 오버타임 이후 레이스 전용 스택 증가 중단을 반영했다.
- 관전 프레임을 생성하지 않는 AI도 자기 전투의 스킬 횟수·초반 전열 손실·승부처 적 전열 체력을 작전 선택에 사용한다.
- 진행도·출주마 자원을 전투 스냅샷에 담아 관전과 재생에서 같은 정보를 표시한다. 전투 요약에 레이스 발동 로그 탭을 추가했다.
- 선택창 키보드 포커스 유지, 작은 화면 줄바꿈, 로컬 선택 타이머/일시정지/자동 선택, 확정 도장 연출을 추가했다. 온라인은 서버 마감 시각을 표시한다.
- 공격·피격·스킬 게인을 0.11→0.16(약 +3.25dB)으로 올리고 마스터 컴프레서로 동시 발음 피크를 제어한다. 기존 음소거와 사용자 음량 설정을 따른다. 레이스 전용 합성 효과음 9종을 추가했다.

## 밸런스 검증 범위

PR25의 코스트 스탯·상점 확률·3성 배율은 유지했다. 이번 수정은 실제 누락된 레이스 효과와 AI 입력을 바로잡는다. 초기 250매치와 최종 재측정은 동일한 대조군이 아니므로 차이를 이번 패치의 인과 효과로 해석하지 않는다.

90노드 구조 검사는 오류 0건이다. [아이템 환산 진단](generated/RACE_PLAN_BUDGET_REVIEW.md)은 78개 검토 표시를 남긴다. 명세 예시 수치와 좁은 IE 예산 범위가 일관되게 맞지 않고, 지원 대상·마나 회전·조건부 효과는 단순 환산이 불가능하므로 엄격한 IE 통과 또는 90개 노드의 동일 승률을 주장하지 않는다. 기본 audit는 구조 오류를 실패 처리하고 추정 경고를 출력한다. --strict-ie는 별도 튜닝용이다.

## 유저/AI 공정성 확인

유저 여부로 공격력·체력·피해량을 바꾸는 분기는 없다. PvP는 동일한 sideFor/전투 엔진을 사용하고, isHuman은 기록할 전투 선택에만 관여한다. statScale은 PvE 몬스터 단계 배율에만 사용한다. 시작 자원, 수입·경험치·구매/리롤 비용 및 PvE 전리품 공식도 공통이다.

장비·증강·레이스 플랜을 가진 동일 편성에서 isHuman만 바꾼 회귀 테스트가 승패/피해/골드/경험치/아이템 보상의 일치를 확인한다. 이는 해당 코드와 테스트 편성의 공정성 검증이며, 사용자가 실제로 패배한 특정 판의 원인을 단정하는 분석은 아니다. AI는 공개된 편성, 자신의 보유 기물·아이템·증강을 평가해 자동으로 조합/배치를 최적화한다.

## 검증 및 재현

타입·린트·데이터·빌드 및 엄격 아트 검사 521/521을 실행했다. 회귀 검사는 tests/race-plan-polish.test.ts, tests/race-plan.test.ts, tests/online.test.ts, tests/browser/race-plan.spec.ts 및 기존 전체 테스트를 포함한다. 재현: npm test / npm run test:ui / npm run simulate -- --season s1 --matches 100 (s1~s5 각각 실행). 최종 커밋의 전체 검사와 재측정 수치는 PR25 본문과 연결된 GitHub Actions 실행에 기록한다.

## 500매치 재측정: 조기 출주 기준 HP 20

| 시즌 | 라스트 3F | 출주 접근 | 평균 종료 | 무승부 |
|---|---:|---:|---:|---:|
| s1 | 99.67% | 89.63% | 5.67 | 3.92% |
| s2 | 99.70% | 88.75% | 5.80 | 3.01% |
| s3 | 99.81% | 87.38% | 5.78 | 1.46% |
| s4 | 99.63% | 89.75% | 5.68 | 0.69% |
| s5 | 99.73% | 89.00% | 5.68 | 0.94% |

출주 접근이 목표 95% 미달이라 명세 §28.7에 따라 RACE_ENTRY_EARLY_HP를 20→30으로 올렸다. 유저와 AI에 동일하게 적용하며 출주 후 능력치 배율은 바꾸지 않는다. 수정 후 5시즌 100매치씩을 CI에서 재측정한다. s4의 선행 우승 점유율 36%는 관찰 항목이다. 다른 시즌까지 일괄 하향할 근거로 단정하지 않고 수정 후 결과를 비교한다.

## 출주 접근성 보완: 저체력 4-1 개방

HP 30만 적용한 재측정에서도 s4는 747/800(93.38%)였다. 미접근 53명은 절대 라운드 16(3-6) 1명, 18(4-1) 16명, 19(4-2) 36명으로, 52명이 기존 조기 개방 직전에 탈락했다. 따라서 **명세의 95% 접근 목표를 달성하기 위해 조기 개방 날짜를 확장**한다: HP≤30일 때 4-1 개방 + 4-3 재확인, 일반 4-5 등록 유지. 4-2 증강과 겹치지 않는다. 등록 보류 의사는 4-3에 강제로 다시 묻지 않는다. 이는 명세 원문의 4-3 단독 조건과 다른, 실측에 근거한 조정이며 별도 무적/피해량 보정은 없다.

전투 후 정산 전 서버 재시작에도 AI 최근 전투 요약을 체크포인트에서 복원하고 한 번만 정산한다. 페노메노 각질 교정에 맞춰 생성 모션 설명서의 VFX 키·서명 2개 필드도 동기화했다.

등록 보류 안내에서 “손해가 없다”는 표현을 제거했다. 등록 이후에야 최종 승부수를 선택해 쓸 수 있음을 명시한다. AI는 보통 즉시 등록하므로 유저가 보류하면 실전 전력 차이가 생길 수 있다. 이는 별도 유저/AI 능력치 보정과 구분한다.
