# Desktop continuation alternatives (2026-09-09)

These ten independently generated 24-pose atlases were made from Race references while another active session completed the same roster segment in commit `1b45c8c3c7762cc9eefb61a8a8b04f1f9a562bbf`.

The remote production atlases are preserved. These variants are **not registered and not counted as additional completed characters**. Each directory preserves the built-in imagegen prompts, generated draft, genuine-alpha extraction, visual review, packed alternative (`runtime.png`), frame crops, uniform scale and SHA-256 provenance. Fine Motion also retains the rejected RGB spacing pass.

Before retaining these as alternatives, the ten local atlases passed the 150-test suite, typecheck, lint and production build; art validation was 400/514 with zero malformed files. The local preview displayed Fine Motion's dash and Agnes Tachyon's 28-unit Canvas skill sequence with zero browser warning/error logs. These results describe the alternatives before remote reconciliation, not a fresh validation of the concurrent remote artwork.

Active continuation has moved to the trailing roster segment recorded in `../desktop-reservation.json` to avoid further duplicate production.
